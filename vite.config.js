import { defineConfig } from 'vite';

export default defineConfig({
  // Relative asset URLs so the build works from a subdirectory such as
  // https://dwarshuis.com/test/KERI-value-system-visualisation/
  base: './',
});
